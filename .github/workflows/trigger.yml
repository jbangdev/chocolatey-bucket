name: Trigger-jbang

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  trigger:
    uses: ./.github/workflows/choco.yml
    with:
      choco-package-name: jbang
      choco-source: https://push.chocolatey.org/
    secrets:
      choco-api-key: ${{ secrets.CHOCOLATEY_API_KEY }}
